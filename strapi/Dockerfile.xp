# =============================================================================
# Dockerfile de developpement pour Strapi
#
# Utilise avec le profil "dev" dans docker-compose.yml
# Le projet entier est monte en volume pour le hot-reload
#
# Usage : docker compose --profile xp up strapi-xp
# =============================================================================
FROM node:20-bookworm-slim

# Installer les dependances de build et outils necessaires
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    python3 \
    msmtp \
    git \
    ca-certificates \
    ffmpeg \
    && ln -sf /usr/bin/msmtp /usr/sbin/sendmail \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

# Activer yarn via corepack
RUN corepack enable && corepack prepare yarn@stable --activate

WORKDIR /app/bob

COPY entrypoint-xp.sh /usr/local/bin/entrypoint-xp.sh
RUN chmod +x /usr/local/bin/entrypoint-xp.sh

EXPOSE 1337

ENTRYPOINT ["entrypoint-xp.sh"]
CMD ["yarn", "develop"]
